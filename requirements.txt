# ── Core ML ──────────────────────────────────────────────────────────────────
# All have pre-built cp314 manylinux wheels — no compilation needed
xgboost==3.3.0
scikit-learn==1.8.0
imbalanced-learn==0.14.2
shap==0.52.0

# ── Data ─────────────────────────────────────────────────────────────────────
pandas==3.0.5
numpy==2.4.3
matplotlib==3.11.0
seaborn==0.13.2

# ── Serialisation ─────────────────────────────────────────────────────────────
joblib==1.5.3

# ── Dashboard (Streamlit Cloud) ───────────────────────────────────────────────
# NOTE: fastapi/pydantic/uvicorn are NOT listed here — they live on the
# Render API service, not on Streamlit Cloud. Including pydantic here causes
# pydantic-core (Rust/PyO3) to be compiled from source, which fails on Py3.14.
streamlit==1.60.0
requests==2.32.3

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
